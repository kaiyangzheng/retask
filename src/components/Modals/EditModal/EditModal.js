import React, {useState} from 'react'
import {
    Modal, 
    Form,
    ButtonToolbar,
    Button,
    Input
} from 'rsuite';
import axiosInstance from '../../../utils/axiosAPI';

export default function EditModal({openEditModal, setOpenEditModal, editTaskId, tasks}){
  return (
    <div>EditModal</div>
  )
}
